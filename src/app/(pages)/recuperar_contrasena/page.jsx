import { RecoverPassword as Form } from "@/app/components/auth/RecoverPassword";

const RecoverPassword = () => {
    return (
        <div className="h-screen bg-gray-100">
            <Form />
        </div>
    );
};

export default RecoverPassword;
